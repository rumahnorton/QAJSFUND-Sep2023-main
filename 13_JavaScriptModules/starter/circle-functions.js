//creating a function area that takes in radius
// within this function it has pi r squared
//this can be used elsewhere because it has the export feature

export function area(r){
    return Math.PI * Math.pow(r,2)
}

